{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Roles</th>
                <th>N° Adhesion</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in listusers %}
        {{ dump(listusers) }}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.username }}</td>
                <td>{{ user.adhesion.firstname }}</td>
                <td>{{ user.adhesion.lastname }}</td>
                <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
                <td>{{ user.adhesion.id }}</td>
                <td>
                    {# <a href="{{ path('user_show', {'id': user.id}) }}">show</a> #}
                    <a href="{{ path('user_updatemajroles', {'id': user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('user_new') }}">Create new</a> #}
{% endblock %}