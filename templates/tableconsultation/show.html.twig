{% extends 'base.html.twig' %}

{% block title %}Tableconsultation{% endblock %}

{% block body %}
    <h1>Tableconsultation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tableconsultation.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ tableconsultation.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ tableconsultation.lastname }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ tableconsultation.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ tableconsultation.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ tableconsultation.roles }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ tableconsultation.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tableconsultation_index') }}">back to list</a>

    <a href="{{ path('tableconsultation_edit', {'id': tableconsultation.id}) }}">edit</a>

    {{ include('tableconsultation/_delete_form.html.twig') }}
{% endblock %}
