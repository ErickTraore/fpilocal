{% extends 'base.html.twig' %}

{% block title %}Fpilyon index{% endblock %}

{% block body %}
    <h1>Fpilyon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Adresse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fpilyon in fpilyons %}
            <tr>
                <td>{{ fpilyon.id }}</td>
                <td>{{ fpilyon.firstname }}</td>
                <td>{{ fpilyon.lastname }}</td>
                <td>{{ fpilyon.email }}</td>
                <td>{{ fpilyon.phone }}</td>
                <td>{{ fpilyon.adresse }}</td>
                <td>
                    <a href="{{ path('fpilyon_show', {'id': fpilyon.id}) }}">show</a>
                    <a href="{{ path('fpilyon_edit', {'id': fpilyon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fpilyon_new') }}">Create new</a>
{% endblock %}
