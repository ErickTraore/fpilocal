{% extends 'base.html.twig' %}

{% block title %}Boss{% endblock %}

{% block body %}
    <h1>Boss</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boss.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ boss.firstName }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ boss.imageName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ boss.lastName }}</td>
            </tr>
            <tr>
                <th>LieuNaissance</th>
                <td>{{ boss.lieuNaissance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('boss_index') }}">back to list</a>

    <a href="{{ path('boss_edit', {'id': boss.id}) }}">edit</a>

    {{ include('boss/_delete_form.html.twig') }}
{% endblock %}
