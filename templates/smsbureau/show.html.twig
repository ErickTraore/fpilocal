{% extends 'base.html.twig' %}

{% block title %}Smsbureau{% endblock %}

{% block body %}
    <h1>Smsbureau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ smsbureau.id }}</td>
            </tr>
            <tr>
                <th>Messagesms</th>
                <td>{{ smsbureau.messagesms }}</td>
            </tr>
            <tr>
                <th>Datesms</th>
                <td>{{ smsbureau.datesms ? smsbureau.datesms|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('smsbureau_index') }}">back to list</a>

    <a href="{{ path('smsbureau_edit', {'id': smsbureau.id}) }}">edit</a>

    {{ include('smsbureau/_delete_form.html.twig') }}
{% endblock %}
