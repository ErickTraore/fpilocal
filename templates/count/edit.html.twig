{% extends 'base.html.twig' %}

{% block title %}Edit Count{% endblock %}

{% block body %}
    <h1>Edit Count</h1>

    {{ include('count/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('count_index') }}">back to list</a>

    {{ include('count/_delete_form.html.twig') }}
{% endblock %}
