{% extends 'base.html.twig' %}

{% block title %}Liste complete{% endblock %}

{% block body %}
 <div class="well">
 <ul>
            <div> <img src="{{ asset('assets/img/logofpi.png') }}" class="img-circle"  alt="logo"></div>
				<h4>FRONT  POPULAIRE  IVOIRIEN</h4>        
				<h6>Représentation FPI-FRANCE</h6>        
			</div>

<caption class="well well-sm"><h2> Liste totale de tous le site</h2></caption>

    <table>

      
    {% if listusers %}
                {# <td class="well well-sm">{{ count.ref }}</td> #}
            <tr>
                <th class="well">Roles:</th>
                <th class="well">N°Tel:</th>
                <th class="well">N°Adher:</th>
                <th class="well">Nom </th>
                <th class="well">Prenom </th>
              
            </tr>
         {% for user in listusers  %}
         <tr> 
                <td class="well well-sm"><a href="{{ path('tablelyon_index') }}">{{ app.user.roles|default|join('') }}</a></td>
                <td class="well well-sm">{{ user.username  | default("") }}</td>
                <td class="well well-sm">{{ user.adhesion.id  | default("") }}</td>
                <td class="well well-sm">{{ user.adhesion.firstname  | default("") }}</td>
                <td class="well well-sm">{{ user.adhesion.lastname  | default("") }}</td>
                
         </tr>
         {% endfor %}
    {% else %}
   Aucune personne ne s'est enregistré sur le site.
{% endif %}
    </table>  
</u>
</div>
<a href="{{ '/' }}"><p><h4><span class="label label-success">Retour</span></h4></p></a>
</div>
{% endblock %} 